let num = prompt("ingrese numeros enteros hasta el 10")
num=parent(num)

if (num <=0 || num > 10) {
    alert("Solo numeros ente 1 y 10")
} else {
    switch (num) {
        case 1:
           
            break;
    
        default:
            break;
    }
}